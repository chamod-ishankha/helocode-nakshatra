# Nakshatra

Sinhala, Tamil and English astrology and almanac app for Sri Lanka — daily horoscope, nekath (auspicious timings), rahu kalaya, panchanga, birth charts and porondam compatibility.

All astronomical calculations run **on-device**. No birth data is uploaded to any server.

> For entertainment purposes only.

## Status

Early development. Not yet released.

## Requirements

| Tool | Version |
| --- | --- |
| Flutter | 3.41.x (stable) |
| Dart | 3.11.x |
| JDK | 21 |
| Android SDK | 37.0.0 |

Verify with `flutter doctor -v` before building.

## Getting started

```bash
git clone https://github.com/chamod-ishankha/helocode-nakshatra.git
cd helocode-nakshatra
flutter pub get
flutter run
```

## Configuration

The following files are **not** in version control and must be supplied locally.
Nothing in this list should ever be committed.

| File | Purpose |
| --- | --- |
| `android/key.properties` | Release signing credentials |
| `android/app/google-services.json` | Firebase Android config |
| `lib/firebase_options.dart` | Generated by FlutterFire CLI |
| `.env` | API keys — see `.env.example` |

`android/local.properties` is generated by Flutter and is also ignored.

## Release signing

Release builds read credentials from `android/key.properties` (git-ignored).
Without it the build still succeeds but is signed with **debug** keys and
Play will reject it — see `android/key.properties.example`.

To create your upload key:

```bash
keytool -genkey -v -keystore android/upload-keystore.jks -storetype JKS -keyalg RSA -keysize 2048 -validity 10000 -alias upload
```

Then copy `android/key.properties.example` to `android/key.properties` and fill
in the passwords you chose.

> Enable **Play App Signing** on the first upload. Google then holds the app
> signing key and this keystore is only the *upload* key, which Google can
> reset if it is ever lost. Without it, losing this file means the app can
> never be updated again.

Back the keystore up somewhere outside this machine.

## Project layout

```
lib/
├── core/        theme, router, constants, error handling
├── features/    feature-first modules (data / domain / presentation)
├── shared/      cross-feature widgets and utilities
└── l10n/        Sinhala, Tamil and English translations
```

## Building

```bash
flutter analyze
flutter test
flutter build appbundle --release
```

Note: `gradlew` and `gradle-wrapper.jar` are intentionally not tracked (Flutter's
default ignore rules). Build through `flutter build`, which regenerates them —
do not invoke `./gradlew` directly on a fresh clone.

## Package identifier

`io.helocode.nakshatra` — permanent. This is bound to the Play Store listing and
cannot be changed after publication.

## Licence

Nakshatra is free software under the **GNU Affero General Public License v3.0
or later**. See [LICENSE](LICENSE).

It links the [Swiss Ephemeris](https://www.astro.com/swisseph/) by Astrodienst
AG, which is dual-licensed AGPL or commercial. We use the AGPL edition, so this
app must be AGPL too and its source must remain available to anyone who receives
a build. That is why this repository is public.

The AGPL permits commercial use: advertising and paid features are fine. The
obligation is source availability, not giving the app away.

If you fork this, your fork must also be AGPL and must publish its source.

### Third-party components

| Component | Author | Licence |
| --- | --- | --- |
| Swiss Ephemeris | Astrodienst AG | AGPL-3.0 |
| IANA Time Zone Database | IANA | Public domain |
| Flutter | Google LLC | BSD-3-Clause |

---

Built by HeloCode Labs.
