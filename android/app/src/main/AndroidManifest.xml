<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Declared explicitly rather than inherited from a dependency's manifest.
         Flutter only adds INTERNET to the debug and profile manifests, so
         without this line the release build silently loses network: no ads,
         no profile backup and no purchase validation, and only after upload.
         Chart and nekath calculation stay offline regardless. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Android 13 and up. Asked for when the user switches a reminder on,
         not at launch: a permission dialog in front of a benefit nobody has
         felt yet is refused, and Android will not ask a second time. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Scheduled alarms do not survive a reboot on their own. Without this
         and the receiver below, reminders stop silently after the phone is
         restarted, which is the failure nobody reports because it looks like
         nothing happening. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Deliberately NOT declared: SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM.
         Play grants those to alarm clocks and calendars, and an almanac
         reminder does not qualify, so asking would risk the listing.
         Scheduling uses inexactAllowWhileIdle instead, which can drift a few
         minutes and still fires in Doze. -->

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Required by the Mobile Ads SDK; the app crashes on start without
             it. Supplied per flavor from build.gradle.kts, so dev and staging
             can never point at live inventory. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />
        <!-- Re-arms everything after a reboot or an app update. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Delivers a scheduled notification when its alarm fires. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
